# 下一步执行清单（数据库优先）

> 目标：回答“现在立刻该做什么”，并按可执行顺序拆成可以直接派工的任务。

## 0. 先做什么（本周唯一主线）

**主线：把后端从 JSON 主存储切到 PostgreSQL 单一真源（SSOT）。**

- 暂停新增业务功能（尤其是新页面、新报表）
- 所有人围绕“建模、迁移、增量接口、切换验证”推进
- 每天只看 3 个指标：迁移一致性、并发冲突正确率、接口错误率

---

## 1. 48 小时内必须完成（可直接开工）

### 1.1 数据库 DDL 冻结
- [ ] 输出并评审 PostgreSQL 建表 SQL（至少包含）：
  - `users`
  - `orders`（含 `version`）
  - `order_dispatches`
  - `order_work_parts`
  - `followups`
  - `finance_sync_logs`
  - `audit_logs`
- [ ] 补索引（`order_id`、`updated_at`、`status`、`sales`、`appointment_time`）
- [ ] 明确字段字典（类型、是否必填、默认值、来源）

### 1.2 迁移脚本骨架
- [ ] 新建迁移脚本目录：`admin-console/scripts/migrate/`
- [ ] 拆 3 个脚本：
  - `migrate_users.py`
  - `migrate_orders.py`
  - `migrate_finance_logs.py`
- [ ] 每个脚本必须支持：`--dry-run`、`--limit`、`--since`

### 1.3 读路径切 DB（不改写路径）
- [ ] API 查询先从 DB 读，JSON 仅兜底
- [ ] 增加 `/health/db`（连通、延迟、错误信息摘要）
- [ ] 记录 SQL 执行耗时日志（P95 可观测）

---

## 2. 第 1 周交付目标（必须可验收）

### 2.1 写路径改造
- [ ] 全量覆盖写 -> `UPSERT`
- [ ] 订单更新接口强制带 `version`
- [ ] 版本冲突返回 `409 ORDER_VERSION_CONFLICT`

### 2.2 增量接口上线
- [ ] `GET /api/v1/orders?updatedAfter=`
- [ ] `PATCH /api/v1/orders/{id}`
- [ ] 后台管理台编辑接口补 `version`

### 2.3 迁移演练
- [ ] 首次全量迁移演练
- [ ] 二次增量迁移演练
- [ ] 输出迁移报告（数量/金额/状态分布对比）

**验收线：**
- [ ] 订单数量差异 = 0
- [ ] 金额汇总差异 = 0
- [ ] 抽样 200 单字段一致率 = 100%

---

## 3. 第 2 周交付目标（切主前必做）

### 3.1 双写与一致性
- [ ] 写 DB + 写 JSON（短期并行）
- [ ] 每小时自动对比一致性
- [ ] 异常自动告警到运维群

### 3.2 切主准备
- [ ] 所有读请求走 DB
- [ ] 发布 Go/No-Go 清单
- [ ] 15 分钟内回滚演练通过

### 3.3 安全兜底（最小闭环）
- [ ] 内部接口强制 token（未配置禁止访问）
- [ ] 密码哈希迁移方案评审通过
- [ ] CORS 收敛为白名单域名

---

## 4. 分工建议（3 人最小配置）

- **后端 A（主责）**：DDL、Repository、增量 API、冲突控制
- **后端 B（配合）**：迁移脚本、校验脚本、双写对比任务
- **前端 C（小程序+管理台）**：`updatedAfter` 拉取、PATCH 携带 version、冲突提示 UI

---

## 5. 每日站会模板（10 分钟）

- 昨天完成：
- 今天计划：
- 当前阻塞：
- 风险等级（高/中/低）：
- 是否影响本周切主（是/否）：

---

## 6. 一句话答案

**下一步就做数据库主线，不要再加功能。今天先冻结 PostgreSQL 表结构和迁移脚本骨架，48 小时内完成读路径切 DB。**
